# HANDOFF — 2026-04-16 14:00

## Session สรุป

### งานที่ทำเสร็จ

#### 1. PDF Scan OCR Fix (Server-side)
- **ไฟล์ใหม่**: `src/lib/ocr/pdf-to-png-server.ts`
  - ใช้ `pdftoppm` (poppler-utils) แปลง PDF → PNG ฝั่ง server
  - DPI 300 สำหรับ OCR quality
  - temp file management ครบวงจร
- **ไฟล์แก้**: `src/lib/ocr/openai-provider.ts`
  - `parsePdf()` เปลี่ยนจากส่ง raw PDF bytes (ผิด MIME) → ใช้ `convertPdfPageToImage()` แปลงเป็น PNG จริงก่อนส่ง Vision API
  - ก่อนหน้า: `data:image/png;base64,{PDF_BYTES}` ← OpenAI ไม่สามารถอ่าน PDF bytes แบบนี้ได้
  - ตอนนี้: PDF → `pdftoppm` → PNG → base64 → Vision API ← ถูกต้อง

#### 2. Upload Receipt Modal — Scrollbar Fix
- **ไฟล์**: `src/app/(app)/expenses/upload-receipt-modal.tsx`
- เปลี่ยน `overflow: "hidden"` → `overflow: "auto"` บน body container
- ทำให้เลื่อนดูข้อมูลใน modal ได้

#### 3. Manual Receipt Modal — 3-Tab Format (ใหม่)
- **ไฟล์ใหม่**: `src/app/(app)/expenses/manual-receipt-modal.tsx`
- 3 Tab เหมือน upload modal: "ข้อมูลหัวบิล" | "รายการค่าใช้จ่าย" | "ข้อมูลการจ่าย"
- ข้อมูลผู้ซื้อ/ผู้ขาย, buyer verification, categories, WHT, summary
- ตัดใบเสนอราคา/ใบแจ้งหนี้ออก — เน้นใบเสร็จ/ใบกำกับภาษี
- **ไฟล์แก้**: `src/app/(app)/expenses/page.tsx`
  - เปลี่ยน Manual button จาก `PaymentModal` → `ManualReceiptModal`

#### 4. OCR Quota Script
- **ไฟล์ใหม่**: `scripts/set-ocr-quota.ts`
- Run: `npx tsx scripts/set-ocr-quota.ts` (ตั้ง scanCredits = 500)
- ⚠️ ยังไม่ได้ run — ต้อง run บนเครื่อง user (Prisma binary mismatch ใน sandbox)

### TypeScript Check
- `npx tsc --noEmit` → **PASS** (0 errors)

---

## ไฟล์ที่แก้ไข/สร้างใหม่

| ไฟล์ | สถานะ |
|---|---|
| `src/lib/ocr/pdf-to-png-server.ts` | NEW — server-side PDF→PNG |
| `src/lib/ocr/openai-provider.ts` | MODIFIED — ใช้ pdftoppm แทน raw bytes |
| `src/app/(app)/expenses/upload-receipt-modal.tsx` | MODIFIED — scrollbar fix |
| `src/app/(app)/expenses/manual-receipt-modal.tsx` | NEW — 3-tab manual receipt |
| `src/app/(app)/expenses/page.tsx` | MODIFIED — ใช้ ManualReceiptModal |
| `scripts/set-ocr-quota.ts` | NEW — ตั้ง OCR quota |

---

## Pending / Action Required

- **⚠️ Run quota script**: `npx tsx scripts/set-ocr-quota.ts` (บนเครื่อง user)
- **⚠️ Subscription table**: อาจยังไม่มีใน DB — ต้อง `npx prisma db push` ก่อน
- **Testing**: ทดสอบ scanned PDF upload อีกครั้งหลัง deploy
- **Future**: R3 ExpenseCategories, R4 LINE draft, R1, R2
