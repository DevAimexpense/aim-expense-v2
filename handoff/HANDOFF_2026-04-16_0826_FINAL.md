# 🤝 SESSION FINAL HANDOFF — 2026-04-16 08:26

> **Read this FIRST in next session!**
>
> **Closing session:** `practical-elegant-knuth` (เอม)
> **Reason:** Context ใกล้เต็ม (~80%) — เริ่ม session ใหม่เพื่อทำ R6 (UI work)

---

## 📚 อ่านลำดับนี้ในเซสชันใหม่

1. **`HANDOFF_2026-04-16_0710_FINAL.md`** — Overall project context, architecture, business rules
2. **`HANDOFF_2026-04-16_0806_REQUIREMENTS.md`** — R1-R7 master spec (Receipt capture & tax compliance) ⭐
3. **`HANDOFF_2026-04-16_0826_FINAL.md`** ← ไฟล์นี้ (state ปัจจุบัน)
4. ไฟล์ HANDOFF อื่น ๆ ในวันนี้ (16 Apr) ตามลำดับ:
   - `_0721.md` — Privacy Policy + ToS (R1.4)
   - `_0732.md` — LINE Hybrid Flex MVP
   - `_0753.md` — LINE PDF support
   - `_0815.md` — R7 Org HQ/Branch
   - `_0825.md` — R5 Payment schema (backend)

---

## ✅ ที่ทำเสร็จในเซสชันนี้

### 1.4 Privacy Policy + Terms of Service ✅
- `/privacy` + `/terms` (Thai, PDPA-compliant)
- Footer link ที่ login + cross-link
- Onboarding checkbox + persist `User.acceptedTermsAt/Version`

### Phase 1B: LINE Hybrid Flex (MVP) ✅
- Webhook `/api/line/webhook` พร้อม signature verification
- Image + PDF + (jpg/png/webp) → OCR → Flex confirm card → save payment status=pending
- Auto-create LINE default Event + Payee
- Drive upload + Payments sheet write
- ⚠️ ใช้ default event/payee ตอนนี้ — R1 จะเปลี่ยนให้ user เลือก project

### R7: Org HQ/Branch ✅
- `Organization.branchType` + `branchNumber` (5 หลัก)
- onboarding form + settings page เพิ่ม UI
- Update mutation + auto force "00000" ถ้า HQ

### R5: Payment Schema (Backend only) ✅
- เพิ่ม 7 columns ใน PAYMENTS sheet: DocumentType, ExpenseNature, CategoryMain, CategorySub, RequesterName, VendorTaxIdSnapshot, VendorBranchInfo
- payment.create/update รับ + persist + auto snapshot vendor info จาก Payee
- LINE handler populate fields ใหม่ตอน confirm
- ⚠️ TTLAmount = ก่อน VAT แล้ว — UI แค่ rename label เป็น "ยอดรวมก่อนภาษี" ไม่ต้องเพิ่ม column ใหม่

---

## ⚠️ Manual Steps ที่พี่ต้องทำก่อนเริ่ม session ใหม่

```bash
cd ~/Desktop/Mac\ Cowork/Aim\ Expense\ V2/aim-expense

# 1. Regen Prisma client (สำคัญ! เพื่อให้ TS รู้จัก fields ใหม่)
npx prisma generate

# 2. Restart dev (clear .next cache)
rm -rf .next && npm run dev
```

ไม่ต้อง prisma db push ใหม่ — push ครบทุก field แล้ว

**ngrok ของ session นี้:** `https://motivator-reaction-obtrusive.ngrok-free.dev`
ถ้าปิด ngrok ไปแล้ว → restart + อัพเดต LINE Console webhook URL ใหม่

---

## 📋 Requirements Checklist (R1-R7)

- [ ] **R1:** LINE Bot เลือก Project ทุกครั้ง (Flex carousel)
- [ ] **R2:** Validation องค์กรเรา + ผู้ขายครบถ้วน
- [ ] **R3:** AI suggest หมวดหมู่ + ExpenseCategories master + เพิ่มเองได้
- [ ] **R4:** Web review หน้าใหม่สำหรับ LINE draft
- [x] **R5:** Backend ครบ ✅
- [ ] **R5b/R6:** UI ของ payment form ใช้ tax fields ใหม่
- [x] **R7:** Org HQ/Branch ✅

---

## 🎯 NEXT TASKS (เริ่ม session ใหม่)

### Priority 1: R6 (UI ตาม R5 backend)
ต่อยอดจาก R5 ทันที — ขยาย UI components ให้ใช้ tax fields ใหม่:
- `ReceiptReviewModal.tsx` — เพิ่ม dropdown DocumentType, ExpenseNature, Category, RequesterName
- Payment create/edit form — เพิ่ม fields เดียวกัน
- Rename label TTLAmount → "ยอดรวมก่อนภาษี" / Subtotal
- เพิ่มคอลัมน์ใน /payments table (อาจซ่อนใน expand row)

### Priority 2: R3 (Categories master)
- Sheet tab ใหม่ `ExpenseCategories` — list หมวดหมู่หลัก/ย่อย per org
- Default seed list (ตามที่กรมสรรพากรยอมรับ — research จาก rd.go.th)
- AI suggest จาก OCR description/items (เพิ่มใน OCR prompt หรือ separate call)
- UI ให้ user เพิ่มหมวดหมู่ใหม่เองได้

### Priority 3+: R4 → R1 → R2

---

## 🐛 Known Issues / Tech Debt

- TSC errors เรื่อง LineDraft/Org branch fields จะหายหลัง prisma generate
- ngrok URL ของ dev session เปลี่ยนทุกครั้งที่ restart — ต้องอัพเดต LINE Console
- `bot_prompt=aggressive` ของ LINE Login ไม่ใช้กับ invite flow (note ใน HANDOFF เก่า)
- LINE OA quota: free tier 200 push messages/เดือน — ถ้าเกินจะ error เงียบ ไม่ส่ง Flex
- LINE webhook ยังไม่เช็ค scan credit quota ของ subscription

---

## 📂 Files Touched (สรุปทั้ง session นี้)

```
prisma/schema.prisma                                       (User legal + LineDraft + Org branch)
src/lib/legal/version.ts                                   NEW
src/app/(legal)/layout.tsx                                 NEW
src/app/(legal)/privacy/page.tsx                           NEW
src/app/(legal)/terms/page.tsx                             NEW
src/app/(auth)/login/page.tsx                              footer link
src/app/(onboarding)/onboarding/company/form.tsx           consent + branch
src/app/api/onboarding/create-company/route.ts             accept + branch + persist
src/app/(app)/settings/org/page.tsx                        select branch fields
src/app/(app)/settings/org/form.tsx                        UI for branch
src/server/routers/org.router.ts                           update mutation + branch
src/server/routers/payment.router.ts                       R5 schema + create + update + toRow
src/server/services/google-sheets.service.ts               R5 columns
src/lib/line/user-org.ts                                   NEW
src/lib/line/defaults.ts                                   NEW
src/lib/line/flex/payment-confirm.ts                       NEW
src/lib/line/handlers.ts                                   NEW (image+PDF+postback+R5)
src/app/api/line/webhook/route.ts                          NEW
```

---

## 💬 Communication Style Reminders

- ภาษาไทย, compact responses, ลงท้าย "ค่ะ"/"คะ"
- ทุก code change → save HANDOFF ใหม่ (ห้ามแก้ไฟล์เก่า)
- เช็ค session capacity ทุก 5-10 turns, เตือนเมื่อ ~70-80%
- Image upload: ย่อ ≤ 1500px ก่อนส่ง

---

ขอบคุณที่ทำงานด้วยกันค่ะพี่ session ใหม่จะรับงานต่อได้สบายเลย 🙏
