# 🤝 HANDOFF 2026-04-16 08:15 — R7: Org HQ/Branch Fields

> **Previous:** `HANDOFF_2026-04-16_0806_REQUIREMENTS.md` (R1-R7 spec)
> **Session:** `practical-elegant-knuth` (เอม)
> **Task:** R7 — เพิ่ม HQ/Branch ในข้อมูลองค์กร ✅

---

## ✅ DONE (R7)

### Schema
- `Organization.branchType` String @default("HQ") — `"HQ" | "Branch"`
- `Organization.branchNumber` String @default("00000") — 5 หลัก
- DB synced ✅

### Backend
- `src/server/routers/org.router.ts` — `update` mutation รับ branchType + branchNumber, validate format, force "00000" ถ้า HQ
- `src/app/api/onboarding/create-company/route.ts` — รับ + persist + auto force "00000" ถ้า HQ

### Frontend
- `src/app/(onboarding)/onboarding/company/form.tsx` — radio "สำนักงานใหญ่/สาขา" + เลขสาขา (แสดงเมื่อ Branch)
- `src/app/(app)/settings/org/form.tsx` — เพิ่ม UI เดียวกันใน settings page
- `src/app/(app)/settings/org/page.tsx` — select branchType + branchNumber

---

## 📋 Requirements Progress

ดู `HANDOFF_2026-04-16_0806_REQUIREMENTS.md` สำหรับ R1-R7 เต็ม ๆ

- [ ] R1: LINE Bot เลือก Project (Flex carousel)
- [ ] R2: Validation องค์กร + ผู้ขาย ครบถ้วน
- [ ] R3: AI suggest หมวดหมู่ + ExpenseCategories master
- [ ] R4: Web review หน้าใหม่สำหรับ LINE draft
- [ ] R5: ขยาย Payment schema ครบ 17 fields
- [ ] R6: Web upload form ครบทุก field
- [x] **R7: Org HQ/Branch + หน้าตั้งค่า** ✅

---

## ⚠️ Manual Step (พี่ต้องทำ)

```bash
cd ~/Desktop/Mac\ Cowork/Aim\ Expense\ V2/aim-expense

# Regen Prisma client (เพราะมี Org.branchType + branchNumber + LineDraft.mimeType)
npx prisma generate
```

**Note:** Org เก่าใน DB จะได้ default values (`HQ` + `00000`) อัตโนมัติจาก `@default()` — ไม่ต้อง backfill

---

## 🧪 Test
- เปิด `/settings/org` → ต้องเห็น radio HQ/Branch + เลือก Branch จะ show เลขสาขา
- เปลี่ยนเป็น Branch → กรอกเลขสาขา 5 หลัก → save → reload → ค่ายังอยู่
- เปลี่ยนกลับ HQ → save → branchNumber กลายเป็น "00000" auto
- onboarding org ใหม่ → ฟอร์มใหม่ ๆ จะมี branch field ตั้งแต่แรก

---

## 🔧 Files Touched (R7)
```
prisma/schema.prisma
src/server/routers/org.router.ts
src/app/api/onboarding/create-company/route.ts
src/app/(onboarding)/onboarding/company/form.tsx
src/app/(app)/settings/org/page.tsx
src/app/(app)/settings/org/form.tsx
```

---

## 📊 Session Status (~65%)

ยังเหลือ ~30-35% — ทำ R5 (ขยาย Payment schema) ต่อได้ในเซสชันนี้ แต่อาจต้อง break เป็นส่วนย่อยเพราะ R5 เกี่ยวข้องหลาย file

**แนะนำลำดับถัดไป:**
- R5 — ขยาย SHEET_HEADERS + Payment schema (กระทบ Sheet หลายที่)
- R3 — ExpenseCategories + AI suggest (ทำต่อจาก R5 ได้ทันที)

---

## 🎯 NEXT TASKS (เลือก 1)

1. **R5: ขยาย Payment schema** ครบ 17 fields ตาม ภพ.30
2. **R3: AI suggest หมวดหมู่** + master list หมวดหมู่
3. ทดสอบ R7 ก่อน
