# 🤝 SESSION HANDOFF — 2026-04-16 11:30

> **Session:** `serene-practical-edison` (เอม)
> **Task:** Upload Receipt Modal + OCR Buyer Fields + Expenses Page Restructure

---

## ✅ ที่ทำเสร็จ (batch นี้)

### 1. OCR Types — เพิ่ม Buyer + Branch Fields
**`src/lib/ocr/types.ts`**
- เพิ่ม `vendorBranch: string | null` — สาขาผู้ขาย
- เพิ่ม `buyerName`, `buyerAddress`, `buyerTaxId`, `buyerBranch` — ข้อมูลผู้ซื้อ

### 2. OCR Prompt — extract ข้อมูลครบ
**`src/lib/ocr/openai-provider.ts`**
- อัพเดท JSON schema ใน prompt ให้ extract:
  - `vendorBranch` — "สำนักงานใหญ่ หรือ สาขา XXXX"
  - `buyerName`, `buyerAddress`, `buyerTaxId`, `buyerBranch`
- อัพเดท `normalize()` ให้ parse buyer tax ID (validate 13 หลัก) + branch fields

### 3. NEW: upload-receipt-modal.tsx (บันทึกค่าใช้จ่าย)
**`src/app/(app)/expenses/upload-receipt-modal.tsx`** — ~570 lines

**เฉพาะ flow ใบเสร็จ/ใบกำกับภาษี** (แยกจาก upload-invoice-modal ที่ใช้กับตั้งเบิก)

#### Features:
- **2-column layout:** ซ้าย = file preview, ขวา = form ทุก field
- **15+ fields ครบถ้วน:**
  1. เลขที่ใบเสร็จ/ใบกำกับภาษี *
  2. วันที่ออกใบเสร็จ *
  3. ประเภทเอกสาร (receipt / tax_invoice)
  4. ค่าใช้จ่ายเป็น (สินค้า / บริการ)
  5. ชื่อผู้ขาย *
  6. เลขผู้เสียภาษีผู้ขาย (validate 13 หลัก)
  7. สาขาผู้ขาย
  8. ที่อยู่ผู้ขาย
  9. ชื่อผู้ซื้อ
  10. เลขผู้เสียภาษีผู้ซื้อ (validate 13 หลัก)
  11. สาขาผู้ซื้อ
  12. ที่อยู่ผู้ซื้อ
  13. รายการสินค้า/บริการ (description)
  14. ยอดก่อน VAT
  15. VAT 7%
  16. ยอดรวมทั้งสิ้น *
  17. หัก ณ ที่จ่าย
  18. หมวดหมู่หลัก/ย่อย
  19. ผู้ขออนุญาตเบิกจ่าย
  20. หมายเหตุ

- **Validation Warnings (real-time):**
  - ⚠️ ยอดในใบเสร็จไม่ตรงกับยอดที่จ่าย
  - ❌ ใบกำกับภาษีต้องมีเลข Tax ID ผู้ขาย/ผู้ซื้อ
  - ❌ เลขผู้เสียภาษีไม่ครบ 13 หลัก
  - ⚠️ ไม่พบเลขที่ใบเสร็จ / วันที่ออกเอกสาร
  - ⚠️ VAT 7% คำนวณไม่ตรง
  - Block save ถ้ามี error-level warning

- **OCR Auto-fill:** ทุก field ถูก fill อัตโนมัติจาก AI อ่าน
- **AI Items Table:** expandable ดูรายการย่อยที่ AI อ่านได้
- **Summary:** ยอดก่อน VAT → VAT → ยอดรวม → หัก ณ ที่จ่าย → ยอดจ่ายจริง
- **Credit badge:** แสดงเครดิต OCR เหลือ

### 4. expenses/page.tsx — อัพเดท
- เปลี่ยน import จาก `UploadInvoiceModal` → `UploadReceiptModal`
- ปุ่ม "📤 อัปโหลดใบเสร็จ" (เดิม "บันทึก Upload")

---

## 📂 Files Changed (batch นี้)

```
src/lib/ocr/types.ts                                    +buyer fields, +vendorBranch
src/lib/ocr/openai-provider.ts                           +buyer extract in prompt/normalize
src/app/(app)/expenses/upload-receipt-modal.tsx           NEW — 570 lines, receipt-specific modal
src/app/(app)/expenses/page.tsx                          import + button wording update
```

---

## ✅ TypeScript Check

```
npx tsc --noEmit → 0 errors ✅
```

---

## ⚠️ Notes

1. **upload-invoice-modal.tsx ยังใช้กับ "ตั้งเบิก" (invoice flow)** — ไม่ได้แก้ไขใน batch นี้
2. **Buyer auto-fill จาก org settings** — ยังไม่ได้ทำ (ไม่มี `org.current` query) → OCR จะ fill ข้อมูลผู้ซื้อจากเอกสารแทน → จะเพิ่ม `org.get` query ภายหลังถ้าต้องการ
3. **upload-invoice-modal** ยังใช้คำว่า "Invoice" — ต้องแก้เฉพาะถ้าพี่ต้องการ

---

## 🎯 NEXT TASKS

### Priority 1: R3 (ExpenseCategories master)
- Sheet tab ใหม่ `ExpenseCategories`
- AI suggest จาก OCR description
- UI ให้ user เพิ่มหมวดหมู่ใหม่

### Priority 2: Org.get query
- เพิ่ม query ดึง org info (name, taxId, address, branchType)
- ใช้ auto-fill buyer fields ใน receipt modal

### Priority 3: R4 (LINE draft web review)

---

## ⚠️ Manual Steps ก่อนเริ่ม session ใหม่

```bash
cd ~/Desktop/Mac\ Cowork/Aim\ Expense\ V2/aim-expense
npx prisma generate
rm -rf .next && npm run dev
```
