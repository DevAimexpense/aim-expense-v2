# 🤝 HANDOFF 2026-04-16 07:21 — Privacy Policy + Terms of Service

> **Previous:** `HANDOFF_2026-04-16_0710_FINAL.md`
> **Session:** `practical-elegant-knuth` (เอม)
> **Task:** 1.4 Privacy Policy + Terms of Service ✅

---

## ✅ DONE in this session

### 1.4 Privacy Policy + Terms of Service (ครบชุด)

#### Schema
- เพิ่ม `User.acceptedTermsAt` (DateTime?) + `User.acceptedTermsVersion` (String?)
- รัน `npx prisma db push` → DB synced ✅
- ⚠️ `prisma generate` ล้มเหลวใน mount (EPERM) → **พี่ต้องรันใน Mac terminal เอง:**
  ```bash
  cd ~/Desktop/Mac\ Cowork/Aim\ Expense\ V2/aim-expense
  npx prisma generate
  ```
  (หรือ restart dev server `rm -rf .next && npm run dev` มักจะ trigger ให้)

#### Files Created
- `src/lib/legal/version.ts` — `LEGAL_VERSION = "2026-04-16"`, COMPANY_NAME, CONTACT_EMAIL
- `src/app/(legal)/layout.tsx` — public layout มี top bar + footer
- `src/app/(legal)/privacy/page.tsx` — นโยบายความเป็นส่วนตัว ภาษาไทย ครบ PDPA + LINE/Google
- `src/app/(legal)/terms/page.tsx` — ข้อกำหนดการใช้งาน ภาษาไทย

#### Files Modified
- `prisma/schema.prisma` — เพิ่ม fields ใน User
- `src/app/(auth)/login/page.tsx` — เพิ่ม footer link Privacy/Terms
- `src/app/(onboarding)/onboarding/company/form.tsx` — เพิ่ม consent checkbox + ส่ง `acceptedTerms: true`
- `src/app/api/onboarding/create-company/route.ts` — รับ `acceptedTerms` + persist `acceptedTermsAt/Version` ใน transaction

#### Validation
- `npx tsc --noEmit` ผ่าน ✅
- ใช้ zod v4 syntax (`{ message: "..." }` แทน `errorMap`)

---

## 📍 Coverage

**หน้าที่เพิ่ม:**
- `/privacy` — public, สอดคล้อง PDPA + LINE consent + Google scope `drive.file`
- `/terms` — public, ครอบคลุม การสมัคร / การใช้งาน / กรรมสิทธิ์ / ข้อจำกัดความรับผิด

**Link จุดต่าง ๆ:**
- ✅ `/login` footer
- ✅ `/privacy` + `/terms` footer (cross link)
- ✅ Onboarding `company` step — checkbox บังคับติ๊ก + ปุ่ม submit disabled จนกว่าจะติ๊ก

**Consent tracking:**
- บันทึก `acceptedTermsAt` + `acceptedTermsVersion` ใน user record ตอน create-company
- เวอร์ชันปัจจุบัน: `2026-04-16` (bump LEGAL_VERSION เมื่อแก้เนื้อหาสาระสำคัญ)

---

## ⚠️ NOT YET (สามารถทำเพิ่มภายหลัง)

- [ ] User เก่าที่ onboard ไปแล้ว → `acceptedTermsAt = null` → อาจเพิ่ม middleware บังคับ accept หากต้องการ strict
- [ ] Re-consent flow เมื่อ bump LEGAL_VERSION (ตอนนี้ยังไม่มี check version mismatch)
- [ ] sitemap + robots.txt (optional, ดีต่อ Google verification)

---

## 🎯 NEXT TASKS (ตาม HANDOFF FINAL)

1. **Phase 1B: LINE Hybrid Flex** — webhook รับรูป → OCR → Flex confirm → save
2. **Phase 1B: Stripe Trial + Billing** — subscription plans + checkout

---

## 🔧 Manual Steps for พี่

1. รัน `npx prisma generate` ใน Mac (เพราะ mount lock)
2. Restart dev: `rm -rf .next && npm run dev`
3. ทดสอบ:
   - เปิด `/privacy` + `/terms` (ไม่ต้อง login)
   - Login → Logout → ดู footer link
   - สร้าง org ใหม่ → ไม่ติ๊ก checkbox → ปุ่มจะ disabled
   - ติ๊ก checkbox → submit → check DB ว่ามี `accepted_terms_at` + `accepted_terms_version`
4. (ถ้าจะ submit LINE Login review) — update Privacy Policy URL ในหน้าตั้งค่า LINE Developers Console
