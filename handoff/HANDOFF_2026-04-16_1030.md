# 🤝 SESSION HANDOFF — 2026-04-16 10:30

> **Session:** `serene-practical-edison` (เอม)
> **Task:** R6 UI + UI Restructure (Sidebar, ตั้งเบิก, บันทึกค่าใช้จ่าย, Approvals Filter)

---

## ✅ ที่ทำเสร็จ (session นี้)

### R6: UI Tax Compliance Fields + Ownership Permission ✅

#### 1. Backend — ownership gate + CreatedByUserId
- **`google-sheets.service.ts`** — เพิ่ม column `CreatedByUserId` ใน PAYMENTS sheet headers
- **`payment.router.ts`** — เพิ่ม:
  - เขียน `CreatedByUserId` ตอน create (ใช้ `ctx.session.userId`)
  - Ownership check ใน `update` mutation: ถ้าไม่ใช่ admin/manager + ไม่ใช่เจ้าของ → throw FORBIDDEN
  - ส่ง `createdByUserId` กลับ UI ใน `toPaymentRow()`

#### 2. Constants
- **`src/lib/constants/expense-categories.ts`** (ใหม่):
  - `EXPENSE_CATEGORIES_MAIN` — 11 หมวดตาม Requirements doc
  - `DOCUMENT_TYPE_OPTIONS` — receipt / tax_invoice
  - `EXPENSE_NATURE_OPTIONS` — goods / service

#### 3. payment-modal.tsx — main create/edit form
- เพิ่ม 5 R5 fields ใน section "🧾 ข้อมูลภาษี"
- Ownership readonly: ถ้า user ไม่ใช่ admin/manager + ไม่ใช่เจ้าของ → 🔒 ดูอย่างเดียว
- Rename TTL → "ยอดรวมก่อนภาษี (Subtotal)", GTTL → "ยอดชำระสุทธิ (รวม VAT หัก WHT)"

#### 4. upload-invoice-modal.tsx — OCR upload form
- เพิ่ม R5 fields compact section "🧾 ข้อมูลภาษี"
- Default documentType = "tax_invoice", RequesterName = ชื่อ user

#### 5. payments/page.tsx — ตั้งเบิก
- Ownership-aware edit/view icon (✏️ vs 👁)
- Category + docType badge
- Rename header/stat labels

### UI Restructure ✅

#### 6. Sidebar (`sidebar.tsx`)
- Group "การเงิน" → renamed to **"ค่าใช้จ่าย"**
- Menu items:
  - **ตั้งเบิก** (💸) → /payments
  - **บันทึกค่าใช้จ่าย** (🧾) → /expenses ← NEW
  - **อนุมัติรายการ** (✅) → /approvals
  - **การเตรียมจ่าย** (💰) → /payment-prep
  - **เอกสาร / ใบเสร็จ** (📄) → /documents

#### 7. payments/page.tsx — Title/Button Rename
- Title: "💸 ตั้งเบิก"
- Buttons: "✏️ ตั้งเบิก Manual" + "📤 ตั้งเบิก Upload"

#### 8. expenses/page.tsx — NEW PAGE (~320 lines)
- **วัตถุประสงค์:** บันทึกค่าใช้จ่ายที่จ่ายไปแล้ว + แนบใบเสร็จกับรายการที่มี
- Stats: จำนวนรายการ, ยังไม่มีใบเสร็จ, มีใบเสร็จแล้ว, ยอดรวม
- Full filter set: search, project, payee, status, type, receipt status, due date range
- Table with receipt status column + upload button for paid items
- Upload + Manual buttons → reuse UploadInvoiceModal & PaymentModal
- Empty state with helpful text

#### 9. approvals/page.tsx — Added Filter Set
- Added filters matching payment-prep: search, project, payee, due date range
- Filter card UI with clear button
- Separated batch actions into own section
- Renamed GTTL → "ยอดชำระ"

---

## 🐛 Bugfixes (session นี้)

### Google Sheet Column Limit
- **ปัญหา:** `ensureAllTabsExist()` เขียน header เกิน grid → "Range exceeds grid limits"
- **Fix:** เพิ่ม `appendDimension` ขยาย grid ก่อนเขียน header

### OCR Quota Reset
- Reset via SQL: `UPDATE subscriptions SET credits_used = 0`
- DB: `postgresql://postgres.gggayyzhmtfvwghagjde:...@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres`

---

## 🔐 Permission Rule (R6)

| Role | ดูรายการ | แก้ไขของตัวเอง | แก้ไขของคนอื่น |
|------|---------|---------------|---------------|
| admin | ✅ | ✅ | ✅ |
| manager | ✅ | ✅ | ✅ |
| accountant | ✅ | ✅ (มี updatePayments) | ❌ |
| staff | ✅ | ❌ (ไม่มี updatePayments) | ❌ |

---

## ⚠️ Manual Steps ก่อนเริ่ม session ใหม่

```bash
cd ~/Desktop/Mac\ Cowork/Aim\ Expense\ V2/aim-expense
npx prisma generate     # (กรณี schema เปลี่ยน)
rm -rf .next && npm run dev
```

**สำคัญ:** หลังจาก run app ครั้งแรก ให้เข้าหน้า Payments → สร้างรายการใหม่ → column `CreatedByUserId` จะถูกเพิ่มอัตโนมัติผ่าน `ensureAllTabsExist()`

---

## 📋 Requirements Checklist (R1-R7)

- [ ] **R1:** LINE Bot เลือก Project ทุกครั้ง (Flex carousel)
- [ ] **R2:** Validation องค์กรเรา + ผู้ขายครบถ้วน
- [ ] **R3:** AI suggest หมวดหมู่ + ExpenseCategories master + เพิ่มเองได้
- [ ] **R4:** Web review หน้าใหม่สำหรับ LINE draft
- [x] **R5:** Backend ครบ ✅
- [x] **R6:** UI ของ payment form ใช้ tax fields ใหม่ ✅
- [x] **R7:** Org HQ/Branch ✅

---

## 🎯 NEXT TASKS

### Priority 1: R3 (Categories master)
- Sheet tab ใหม่ `ExpenseCategories` — list หมวดหมู่หลัก/ย่อย per org
- Default seed list (research จาก rd.go.th)
- AI suggest จาก OCR description
- UI ให้ user เพิ่มหมวดหมู่ใหม่เองได้

### Priority 2: R4 (Web review for LINE draft)
- หน้าใหม่: `/payments/line-draft/[draftId]`
- Review + edit + save → Google Sheet

### Priority 3+: R1 → R2

---

## 📂 Files Touched (session นี้)

```
src/lib/constants/expense-categories.ts              NEW — 11 categories + type options
src/server/services/google-sheets.service.ts          +CreatedByUserId column + appendDimension fix
src/server/routers/payment.router.ts                  +ownership gate + CreatedByUserId write
src/app/(app)/payments/payment-modal.tsx               R5 fields + ownership readonly + label rename
src/app/(app)/payments/upload-invoice-modal.tsx         R5 fields compact + requester default
src/app/(app)/payments/page.tsx                        renamed ตั้งเบิก + ownership edit/view + R5 badges
src/app/(app)/expenses/page.tsx                        NEW — บันทึกค่าใช้จ่าย page
src/app/(app)/approvals/page.tsx                       added full filter set + batch actions section
src/components/layout/sidebar.tsx                      restructured nav groups + new expenses menu
```

---

## ✅ TypeScript Check

```
npx tsc --noEmit → 0 errors ✅
```
