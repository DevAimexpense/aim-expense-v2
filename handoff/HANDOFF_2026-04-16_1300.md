# HANDOFF — 2026-04-16 13:00

## Session สรุป

### งานที่ทำเสร็จใน session นี้

#### 1. Upload Receipt Modal — Paypers-style Rewrite
- **ไฟล์**: `src/app/(app)/expenses/upload-receipt-modal.tsx`
- สร้าง modal ใหม่ แยกจาก upload-invoice-modal (ใช้เฉพาะหน้า "บันทึกค่าใช้จ่าย")
- Tab UI 3 แท็บ: "ข้อมูลหัวบิล" | "รายการค่าใช้จ่าย" | "ข้อมูลการจ่าย"
- Project selector อยู่ด้านบนก่อน tabs (เด่นชัด)
- Buyer verification — เทียบข้อมูลผู้ซื้อจาก OCR กับข้อมูลองค์กร (org.get)
- Per-item expense card พร้อม category/type radio (สินค้า/บริการ)
- Summary section (ยอดชำระ, ยอดก่อนภาษี, VAT, WHT)
- Validation warnings พร้อม red dot บน tab
- OCR auto-fill ทุก field

#### 2. PDF Scan Support (Bug Fix)
- **ไฟล์**: `src/lib/utils/pdf-to-image.ts` (NEW)
- Client-side: pdfjs-dist render PDF → canvas → PNG ก่อนส่ง OCR
- **ไฟล์**: `src/lib/ocr/openai-provider.ts`
- Server-side fallback: scanned PDF ส่ง raw bytes เป็น base64 ไป Vision API แทน throw error

#### 3. OCR Enhancement — Buyer Fields + Branch
- **ไฟล์**: `src/lib/ocr/types.ts`
- เพิ่ม: buyerName, buyerAddress, buyerTaxId, buyerBranch, vendorBranch
- **ไฟล์**: `src/lib/ocr/openai-provider.ts`
- Prompt + normalize() รองรับ field ใหม่ทั้งหมด

#### 4. Org Router — get query
- **ไฟล์**: `src/server/routers/org.router.ts`
- เพิ่ม `org.get` query ดึง name, taxId, address, phone, branchType, branchNumber
- ใช้ใน receipt modal สำหรับ buyer verification

#### 5. TypeScript Fix
- **ไฟล์**: `src/lib/utils/pdf-to-image.ts`
- แก้ pdfjs-dist v5 RenderParameters ที่ต้องการ `canvas` property

### TypeScript Check
- `npx tsc --noEmit` → **PASS** (0 errors)

---

## ไฟล์ที่แก้ไข/สร้างใหม่

| ไฟล์ | สถานะ |
|---|---|
| `src/app/(app)/expenses/upload-receipt-modal.tsx` | REWRITTEN (Paypers-style) |
| `src/app/(app)/expenses/page.tsx` | MODIFIED (import ReceiptModal) |
| `src/app/(app)/payments/upload-invoice-modal.tsx` | MODIFIED (PDF→image) |
| `src/lib/utils/pdf-to-image.ts` | NEW |
| `src/lib/ocr/types.ts` | MODIFIED (buyer fields) |
| `src/lib/ocr/openai-provider.ts` | MODIFIED (buyer + PDF fallback) |
| `src/server/routers/org.router.ts` | MODIFIED (org.get query) |

---

## Pending / Future Tasks

### ยังไม่ได้ทำ
- **R3**: ExpenseCategories master — seed list, AI suggest, user add custom
- **R4**: LINE draft web review
- **R1, R2**: (จาก requirements doc)
- **Testing**: ทดสอบ PDF→image กับ scanned PDF จริง (pdfjs worker อาจมี issue ใน Next.js)
- **Testing**: ทดสอบ buyer verification flow กับ org data จริง

### Architecture Notes
- `upload-receipt-modal.tsx` = ใช้กับหน้า "บันทึกค่าใช้จ่าย" (expenses)
- `upload-invoice-modal.tsx` = ใช้กับหน้า "ตั้งเบิก" (payments)
- ทั้งสอง modal ใช้ `pdfFirstPageToImage()` สำหรับ PDF conversion
- OCR ใช้ OpenAI GPT-4o Vision เป็นหลัก
