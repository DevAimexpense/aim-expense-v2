# 🤝 SESSION HANDOFF — 2026-04-16 09:30

> **Session:** `serene-practical-edison` (เอม)
> **Task:** R6 — UI ขยายตาม R5 backend + ownership permission

---

## ✅ ที่ทำเสร็จ

### R6: UI Tax Compliance Fields + Ownership Permission ✅

#### 1. Backend — ownership gate + CreatedByUserId
- **`google-sheets.service.ts`** — เพิ่ม column `CreatedByUserId` ใน PAYMENTS sheet headers
- **`payment.router.ts`** — เพิ่ม:
  - เขียน `CreatedByUserId` ตอน create (ใช้ `ctx.session.userId`)
  - Ownership check ใน `update` mutation: ถ้าไม่ใช่ admin/manager + ไม่ใช่เจ้าของ → throw FORBIDDEN
  - ส่ง `createdByUserId` กลับ UI ใน `toPaymentRow()`

#### 2. Constants
- **`src/lib/constants/expense-categories.ts`** (ใหม่):
  - `EXPENSE_CATEGORIES_MAIN` — 11 หมวดตาม Requirements doc
  - `DOCUMENT_TYPE_OPTIONS` — receipt / tax_invoice
  - `EXPENSE_NATURE_OPTIONS` — goods / service

#### 3. payment-modal.tsx — main create/edit form
- เพิ่ม 5 R5 fields ใน section "🧾 ข้อมูลภาษี":
  - DocumentType (select: ใบเสร็จ / ใบกำกับภาษี)
  - ExpenseNature (select: สินค้า / บริการ)
  - CategoryMain (dropdown 11 หมวด)
  - CategorySub (text input)
  - RequesterName (text, default = ชื่อ user ปัจจุบัน)
- Vendor snapshot display (read-only) เมื่อ edit
- Rename TTL → "ยอดรวมก่อนภาษี (Subtotal)"
- Rename GTTL → "ยอดชำระสุทธิ (รวม VAT หัก WHT)"
- **Ownership readonly**: ถ้า user ไม่ใช่ admin/manager + ไม่ใช่เจ้าของ → 🔒 ดูอย่างเดียว badge + disable ทุก input
- ส่ง R5 fields ไป backend ตอน submit

#### 4. upload-invoice-modal.tsx — OCR upload form
- เพิ่ม R5 fields compact section "🧾 ข้อมูลภาษี"
- Default documentType = "tax_invoice" (เพราะ upload invoice)
- RequesterName default = ชื่อ user จาก org.me
- ส่ง R5 fields ผ่าน sharedBase → backend

#### 5. payments/page.tsx — table view
- Ownership-aware edit/view icon (✏️ vs 👁)
- Category + docType badge ใน description cell
- Rename header "ยอด (GTTL)" → "ยอดชำระ"
- Rename stat sub "GTTL Total" → "ยอดชำระสุทธิ"

---

## 🔐 Permission Rule (R6)

| Role | ดูรายการ | แก้ไขของตัวเอง | แก้ไขของคนอื่น |
|------|---------|---------------|---------------|
| admin | ✅ | ✅ | ✅ |
| manager | ✅ | ✅ | ✅ |
| accountant | ✅ | ✅ (มี updatePayments) | ❌ |
| staff | ✅ | ❌ (ไม่มี updatePayments) | ❌ |

Note: accountant.updatePayments = true (ไม่เปลี่ยน) — แต่ถูก ownership gate block ไม่ให้แก้ของคนอื่น

---

## 🐛 Bugfix — Google Sheet Column Limit (session นี้)

**ปัญหา:** `ensureAllTabsExist()` พยายามเขียน header ไปที่คอลัมน์ที่เกิน grid → error "Range exceeds grid limits" → ทำให้ทุก query พัง (company bank, payments ไม่แสดง)

**Fix:** เพิ่ม `appendDimension` request ขยาย grid ก่อนเขียน header ใน `google-sheets.service.ts`

**ข้อมูลเดิม:** ไม่ได้หาย — แค่ query error ก่อน return. หลัง fix + restart ข้อมูลจะกลับมา

---

## ⚠️ Manual Steps ก่อนเริ่ม session ใหม่

```bash
cd ~/Desktop/Mac\ Cowork/Aim\ Expense\ V2/aim-expense
npx prisma generate     # (กรณี schema เปลี่ยน)
rm -rf .next && npm run dev
```

**สำคัญ:** หลังจาก run app ครั้งแรก ให้เข้าหน้า Payments → สร้างรายการใหม่ → column `CreatedByUserId` จะถูกเพิ่มอัตโนมัติผ่าน `ensureAllTabsExist()`

---

## 📋 Requirements Checklist (R1-R7)

- [ ] **R1:** LINE Bot เลือก Project ทุกครั้ง (Flex carousel)
- [ ] **R2:** Validation องค์กรเรา + ผู้ขายครบถ้วน
- [ ] **R3:** AI suggest หมวดหมู่ + ExpenseCategories master + เพิ่มเองได้
- [ ] **R4:** Web review หน้าใหม่สำหรับ LINE draft
- [x] **R5:** Backend ครบ ✅
- [x] **R6:** UI ของ payment form ใช้ tax fields ใหม่ ✅
- [x] **R7:** Org HQ/Branch ✅

---

## 🎯 NEXT TASKS

### Priority 1: R3 (Categories master)
- Sheet tab ใหม่ `ExpenseCategories` — list หมวดหมู่หลัก/ย่อย per org
- Default seed list (research จาก rd.go.th)
- AI suggest จาก OCR description
- UI ให้ user เพิ่มหมวดหมู่ใหม่เองได้

### Priority 2: R4 (Web review for LINE draft)
- หน้าใหม่: `/payments/line-draft/[draftId]`
- Review + edit + save → Google Sheet

### Priority 3+: R1 → R2

---

## 📂 Files Touched (session นี้)

```
src/lib/constants/expense-categories.ts              NEW — 11 categories + type options
src/server/services/google-sheets.service.ts          +CreatedByUserId column
src/server/routers/payment.router.ts                  +ownership gate + CreatedByUserId write + toPaymentRow
src/app/(app)/payments/payment-modal.tsx               R5 fields + ownership readonly + label rename
src/app/(app)/payments/upload-invoice-modal.tsx         R5 fields compact + requester default
src/app/(app)/payments/page.tsx                        ownership edit/view + R5 badges + label rename
```
